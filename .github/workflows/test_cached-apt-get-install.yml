name: Test cached-apt-get-install

on:

  workflow_dispatch:

jobs:

  test:

    name: Test cached-apt-get-install

    runs-on: ubuntu-latest

    steps:

      - name: Run cached-apt-get-install composite action
        uses: oslokommune/composite-actions/cached-apt-get-install@827fea6ee57b1937916eb54f9d6b0c728c1f6c66 # main
        with:
          packages: age
