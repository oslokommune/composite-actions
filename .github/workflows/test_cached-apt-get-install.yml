name: Test cached-apt-get-install

on:

  workflow_dispatch:

jobs:

  test:

    name: Test cached-apt-get-install

    runs-on: ubuntu-latest

    steps:

      - name: Run cached-apt-get-install composite action
        uses: oslokommune/composite-actions/cached-apt-get-install@55ee2c7bef4b80f4535761f03b8f3d07e064836f # main
        with:
          packages: age
