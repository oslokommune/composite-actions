name: Test optimize-apt-get

on:

  workflow_dispatch:

jobs:

  test:

    name: Test optimize-apt-get

    runs-on: ubuntu-latest

    steps:

      - name: Run optimize-apt-get composite action
        uses: oslokommune/composite-actions/optimize-apt-get@bdba791d662bb8d48b2f1105430a01cde846277b # main


      - name: Install age
        run: |
          sudo apt-get update
          sudo apt-get install age
